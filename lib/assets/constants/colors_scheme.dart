import 'package:flutter/material.dart';

class ColorsScheme {
  static const Color neuroBlue = Color.fromRGBO(124, 156, 191, 1);
  static const Color neuroBlueLight = Color.fromRGBO(212, 228, 247, 1);

  // Fundos
  static const Color backgroundLight = Color.fromRGBO(250, 250, 250, 1);
  static const Color backgroundDark = Color.fromRGBO(26, 29, 35, 1); // Padrão Dark Mode

  // Bottom Bar Fundos
  static const Color navBarBackgroundLight = Color.fromRGBO(250, 250, 250, 1);//
  static const Color navBarBackgroundDark = Color.fromRGBO(36, 40, 49, 1);//

  static const Color foregroundLight = Color.fromRGBO(250, 250, 250, 1);
  static const Color foregroundDark = Color(0xFF161616);

  // Textos
  static const Color textLight = Colors.black;
  static const Color textDark = Colors.white;
  static const Color textGrey = Color.fromRGBO(0, 0, 0, 0.76);
  static const Color textBlueGrey = Colors.blueGrey;

  // Ícones
  static const Color iconLight = Colors.black54;
  static const Color iconDark = Colors.white70;
}
